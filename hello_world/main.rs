// This is a simple Rust program that prints "Hello, world!" to the console.
// To run this program, run rustc main.rs to compile it, and then execute the generated binary.

fn main(){
    println!("Hello, world!");
}

// cargo --version
// This command checks the version of Cargo, which is Rust's package manager and build system.

// cargo new hello_world
// This command creates a new Rust project named "hello_world". It sets up the directory structure and initializes a Git repository.

// cargo build
// This command compiles the Rust project. It generates an executable binary in the target/debug directory

// cargo run
// This command compiles the project (if necessary) and then runs the generated binary. In this case, it will execute the main function and print "Hello, world!" to the console.

// cargo check